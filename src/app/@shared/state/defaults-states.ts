import { AUTH_STATE_DEFAULTS } from '@app/auth/state';
import { POSTS_STATE_DEFAULTS } from '@app/modules/post/state';

export const DEFAULTS_STATES = {
  auth: AUTH_STATE_DEFAULTS,
  posts: POSTS_STATE_DEFAULTS,
};
